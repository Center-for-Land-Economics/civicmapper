## What

Brief description of the change.

## Why

Motivation / linked issue (e.g. `Fixes #123`).

## How verified

- [ ] `cd api && npm test` passes
- [ ] `cd viz && npm run typecheck` passes
- [ ] `cd viz && npm run build` passes
- [ ] Verified in the running app (screenshot for visual changes)

## Notes for reviewers

Anything that needs special attention.
